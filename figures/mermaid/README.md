# Mermaid Diagrams

Place your Mermaid source files (.mmd) here.

## Rendering

Use the mermaid.ink API or mermaid-cli to render:

```bash
# Using mermaid-cli
npm install -g @mermaid-js/mermaid-cli
mmdc -i diagram.mmd -o ../rendered/diagram.png
```

Or use the provided `render_mermaid.py` script.
